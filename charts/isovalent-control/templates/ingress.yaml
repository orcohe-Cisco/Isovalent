{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "ic.fullname" . }}
  labels: {{- include "ic.labels" . | nindent 4 }}
  {{- with .Values.ingress.annotations }}
  annotations: {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- with .Values.ingress.tls }}
  tls: {{- toYaml . | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: {{ include "ic.fullname" . }}-backend
                port: { number: {{ .Values.service.backendPort }} }
          - path: /ws
            pathType: Prefix
            backend:
              service:
                name: {{ include "ic.fullname" . }}-backend
                port: { number: {{ .Values.service.backendPort }} }
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "ic.fullname" . }}-frontend
                port: { number: {{ .Values.service.frontendPort }} }
{{- end }}
